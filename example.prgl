import System from prgl;
import System.Flow from prgl;
import ArrayUtils from prgl as Array;

define i = [0, 1];

System.Console.print($i[1]);
Array.set("i", 0, $i[0] + 1);
Array.set("i", 1, $i[1] * 2);

Flow.jump($line - 3, $i[0] <= $argv[0]);

Flow.end();
System.Console.print("don`t print");