import System from prgl;
import Flow from prgl;

define i = 0;
define g = 1;
define x = argv[0];

System.Console.print(g);
define i = $i + 1;
define g = $g * 2;
Flow.jump(5, $i <= $x);

Flow.end();
System.Console.print("don`t print");