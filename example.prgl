import System.Console.print from prgl;
import System.Flow from prgl;

define i = [0, 1];

print($i[1]);
define i[0] = $i[0] + 1;
define i[1] = $i[1] * 2;

Flow.jump($line - 3, $i[0] <= $argv[0]);

Flow.end();
print("don`t print");